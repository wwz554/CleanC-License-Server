# CleanC License Server 生产环境保姆级安装部署教程

> 适用版本：生产协议 API v3 / Lease v4
>
> 部署方式：Cloudflare Pages 直接连接 GitHub + Pages Functions + D1 + Turnstile
>
> 默认 Lease：72 小时
>
> 授权规则：一个授权码同一时间只允许绑定一台设备；一台设备同一时间只允许占用一个有效授权。

---

# 0. 先看最终效果

部署完成后，你会得到：

```text
https://你的项目名.pages.dev/admin
```

这是授权管理后台。

客户端使用：

```text
https://你的项目名.pages.dev/bootstrap/v1/config
```

作为永久 Bootstrap 地址。

正式生产 API 可以继续使用 pages.dev，也可以以后绑定自己的域名，例如：

```text
https://license.example.com
```

Cloudflare Pages 会直接连接 GitHub 仓库：

```text
wwz554/CleanC-License-Server
```

以后 GitHub main 分支有新提交，Cloudflare 会自动重新构建和部署。

---

# 1. 生产授权规则

当前服务端最终规则如下。

```text
一个授权码 = 同一时间最多绑定 1 台设备
一台设备 = 同一时间最多占用 1 个有效授权
```

管理员后台点“解绑”后：

```text
该授权码可以绑定另一台设备
原设备也可以绑定其他授权码
```

如果两个设备同时抢同一个授权码：

```text
第一个成功取得 D1 激活锁的请求成功
其他并发请求立即失败
```

不会出现一个授权码同时绑定两台设备。

---

# 2. 三种授权类型

## 2.1 永久授权

永久授权：

```text
licenseType = permanent
licenseExpiresAt = null
isPermanent = true
displayText = 永久授权
```

Windows 客户端右下角只显示：

```text
永久授权
```

不要显示 72 小时倒计时。

72 小时只是后台 Lease 刷新周期，和永久授权总期限没有关系。

---

## 2.2 激活后 N 天

例如创建 7 天授权：

```text
创建授权码
↓
无人使用
↓
一直保持“完整 7 天”
↓
第一次真正成功激活
↓
activated_at = 首次激活时间
expires_at = 首次激活时间 + 7 天
```

所以：

```text
今天创建 7 天授权
20 天后客户才第一次使用
客户仍然从第一次激活开始拥有完整 7 天
```

第二台设备、管理员解绑、重新绑定，都不会重新计算第一次激活时间。

---

## 2.3 固定到期

例如：

```text
2026-12-31 23:59:59 到期
```

这个授权无论有没有激活，到该时间都到期。

---

# 3. 72 小时 Lease 是什么

Cloudflare Pages 普通情况下不需要被客户端频繁访问。

默认：

```text
LEASE_HOURS=72
```

客户端首次激活成功后拿到服务器签名 Lease。

Lease 有效期间：

```text
不需要每次启动联网
不需要每次清理 C 盘联网
不需要每几分钟请求一次服务器
```

客户端只需要在 Lease 快到期时执行两次小请求：

```text
① POST /api/v1/device/challenge
② POST /api/v1/license/refresh
```

旧版：

```text
validate
device/verify
deviceProof
```

已经废弃。

---

# 4. 准备工作

你需要：

```text
1. GitHub 账号
2. Cloudflare 账号
3. GitHub 仓库：wwz554/CleanC-License-Server
4. 一个 D1 数据库
5. 一个 Turnstile Widget
6. 一组 P-256 服务器签名密钥
```

不需要：

```text
Cloudflare API Token
GitHub Actions 部署 Token
D1 database_id 写进代码
手工执行建表 SQL
wrangler deploy
```

---

# 5. Cloudflare Pages 连接 GitHub

Cloudflare 官方 Pages Git Integration 支持 GitHub 自动部署。

Cloudflare 控制台进入：

```text
Workers & Pages
```

然后选择：

```text
Create application
→ Pages
→ Connect to Git / Import an existing Git repository
```

第一次连接 GitHub 时，Cloudflare 会要求安装 GitHub App。

授权访问仓库：

```text
wwz554/CleanC-License-Server
```

然后选择该仓库。

---

# 6. Pages 构建参数怎么填

Production branch：

```text
main
```

Framework preset：

```text
None
```

Build command：

```text
npm run build
```

Build output directory：

```text
public
```

Root directory：

```text
留空
```

完整示例：

```text
Production branch: main
Framework preset: None
Build command: npm run build
Build output directory: public
Root directory: （空）
```

然后点击：

```text
Save and Deploy
```

第一次部署时还没绑定 D1，接口可能返回：

```json
{
  "success": false,
  "code": "D1_BINDING_MISSING"
}
```

这是正常现象。

---

# 7. 创建 D1 数据库

Cloudflare 控制台进入：

```text
Storage & Databases
→ D1 SQL Database
→ Create database
```

数据库名字建议填写：

```text
cleanc-license
```

名字只是示例，你也可以填写：

```text
cleanc-prod-license
```

但 Pages Binding 的变量名必须固定为：

```text
DB
```

---

# 8. 把 D1 绑定到 Pages

进入：

```text
Workers & Pages
→ 你的 CleanC Pages 项目
→ Settings
→ Bindings
→ Add
→ D1 database
```

填写：

```text
Variable name: DB
D1 database: cleanc-license
```

这里最重要的是：

```text
DB
```

必须完全一致，区分大小写。

错误示例：

```text
D1
DATABASE
CLEANC_DB
DB1
```

都不能用。

Cloudflare 官方说明：Pages Functions 会通过 `context.env.<变量名>` 访问 D1；本项目代码使用的是 `env.DB`。

绑定 D1 后必须重新部署一次：

```text
Deployments
→ 最新 Production deployment
→ Retry deployment
```

也可以直接向 GitHub main 再提交一次代码触发自动部署。

---

# 9. D1 不需要手工建表

重新部署后，第一次正常请求会自动创建/升级数据库。

会自动建立：

```text
licenses
devices
audit_logs
system_settings
domain_history
device_challenges
rate_limits
activation_locks
```

还会自动建立：

```text
一个授权码只能有一个活动设备的唯一索引
一台设备只能有一个活动授权的唯一索引
授权生命周期保护 Trigger
```

所以不要自己手工创建这些表。

现有数据库重新部署也不会清空授权码和设备记录。

---

# 10. 创建 Cloudflare Turnstile

Cloudflare 控制台进入：

```text
Turnstile
→ Add widget
```

Widget name 示例：

```text
CleanC License Admin
```

Hostname 添加你的 Pages 主机名。

例如你的 Pages 地址是：

```text
https://cleanc-license-server.pages.dev
```

Hostname 只填写：

```text
cleanc-license-server.pages.dev
```

不要填写：

```text
https://
```

不要填写路径：

```text
/admin
```

以后绑定正式域名：

```text
license.example.com
```

记得也把：

```text
license.example.com
```

加入 Turnstile Hostname Management。

创建完成会得到：

```text
Site Key
Secret Key
```

Site Key 可以公开。

Secret Key 必须加密保存。

---

# 11. Pages Variables and Secrets

进入：

```text
Workers & Pages
→ 你的 CleanC 项目
→ Settings
→ Variables and Secrets
→ Add
```

Cloudflare 官方支持在这里设置普通环境变量和加密 Secret。

---

# 12. 普通变量怎么填

## APP_NAME

变量名：

```text
APP_NAME
```

值：

```text
CleanC
```

一般不用改。

---

## TURNSTILE_SITE_KEY

变量名：

```text
TURNSTILE_SITE_KEY
```

示例：

```text
0x4AAAAAAAxxxxxxxxxxxxxxxxxx
```

这是 Turnstile 页面显示的 Site Key。

它本身是公开值，所以普通变量即可。

---

## LEASE_HOURS

变量名：

```text
LEASE_HOURS
```

生产建议：

```text
72
```

含义：

```text
72 小时内客户端主要使用本地签名 Lease
快到期时才联网 challenge + refresh
```

不要把永久授权理解成只有 72 小时。

永久授权右下角仍然显示：

```text
永久授权
```

---

## BOOTSTRAP_BASE_URL

变量名：

```text
BOOTSTRAP_BASE_URL
```

一定填写 Pages 自带的稳定地址。

示例：

```text
https://cleanc-license-server.pages.dev
```

不要填写某次 Preview deployment 地址。

不要在最后加 `/`。

这个地址建议永远保留在 Windows 客户端里作为固定入口。

---

# 13. 加密 Secret 怎么填

添加以下变量时必须选择：

```text
Encrypt
```

需要的 Secret：

```text
ADMIN_PASSWORD
SESSION_SECRET
TURNSTILE_SECRET
LICENSE_SIGNING_PRIVATE_KEY
```

当前生产协议不再需要：

```text
DEVICE_PROOF_SECRET
```

---

# 14. ADMIN_PASSWORD

变量名：

```text
ADMIN_PASSWORD
```

示例格式：

```text
CleanC_Admin_9v!A5q_2026
```

上面只是格式示例，不要直接照抄当生产密码。

正式建议：

```text
至少 16 位
大小写字母
数字
特殊字符
不要和 GitHub/Cloudflare 密码相同
```

选择：

```text
Encrypt
```

---

# 15. SESSION_SECRET

变量名：

```text
SESSION_SECRET
```

这是后台 Session 和 CSRF HMAC 密钥。

Windows 安装了 Git Bash/OpenSSL 后执行：

```bash
openssl rand -base64 48
```

会得到类似：

```text
0wQ2ZC8Kxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx=
```

每个人生成结果都不同。

把你自己生成的完整值复制进去。

选择：

```text
Encrypt
```

不要把生成结果提交 GitHub。

---

# 16. TURNSTILE_SECRET

变量名：

```text
TURNSTILE_SECRET
```

填写 Turnstile 页面给你的 Secret Key。

示例格式：

```text
0x4AAAAAAAxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

必须选择：

```text
Encrypt
```

不要和 Site Key 填反。

---

# 17. 生成服务器 P-256 签名密钥

这一步非常重要。

服务器使用私钥签名：

```text
Bootstrap
License Lease
```

Windows 客户端只内置公钥。

私钥绝对不能放到 Windows 客户端。

在 Git Bash / Linux / macOS 终端执行：

```bash
openssl ecparam -name prime256v1 -genkey -noout -out ec-private-sec1.pem
openssl pkcs8 -topk8 -nocrypt -in ec-private-sec1.pem -out cleanc-private.pem
openssl pkey -in cleanc-private.pem -pubout -out cleanc-public.pem
```

生成：

```text
cleanc-private.pem
cleanc-public.pem
```

---

# 18. LICENSE_SIGNING_PRIVATE_KEY

变量名：

```text
LICENSE_SIGNING_PRIVATE_KEY
```

打开：

```text
cleanc-private.pem
```

内容类似：

```text
-----BEGIN PRIVATE KEY-----
MIGHAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBG0wawIBAQQg......
......
-----END PRIVATE KEY-----
```

在 Cloudflare Secret 输入框中直接粘贴完整内容，包括：

```text
-----BEGIN PRIVATE KEY-----
-----END PRIVATE KEY-----
```

和中间换行。

必须选择：

```text
Encrypt
```

这个私钥：

```text
不能上传 GitHub
不能发给客户
不能打包到 exe
不能放前端 JavaScript
```

---

# 19. 客户端应该放哪个密钥

客户端只放：

```text
cleanc-public.pem
```

这是公钥，可以随软件分发。

客户端使用它验证服务器返回的：

```text
signedPayload
signature
```

一定要对服务器返回的 `signedPayload` 原始字节进行验签。

不要：

```text
把 JSON 解析后重新序列化，再对重新生成的 JSON 验签
```

正确顺序：

```text
Base64URL 解码 signedPayload
↓
得到服务器真正签名的原始 UTF-8 字节
↓
使用服务器 P-256 公钥验证 signature
↓
验签成功后
↓
再解析 JSON
```

---

# 20. .NET / Windows ECDSA 特别注意

Cloudflare WebCrypto 的 ECDSA 签名需要 Windows 客户端和服务端使用一致的签名格式。

如果客户端使用 .NET `ECDsa`，建议明确使用：

```text
DSASignatureFormat.IeeeP1363FixedFieldConcatenation
```

设备私钥签 nonce 时同样保持一致。

例如逻辑应是：

```text
ECDSA P-256
SHA-256
P1363 固定字段拼接格式
```

不要一边使用 DER，一边使用 P1363，否则会出现：

```text
授权码正确
设备也正确
但签名一直验证失败
```

---

# 21. 所有变量填完后重新部署

Variables / Secrets / D1 Binding 改完以后：

```text
Deployments
→ 最新 Production deployment
→ Retry deployment
```

或者 GitHub main 新提交也会自动部署。

---

# 22. 第一项检查：Health

打开：

```text
https://你的项目.pages.dev/api/v1/health
```

生产版本应该看到：

```json
{
  "status": "ok",
  "service": "cleanc-license-server",
  "apiVersion": 3,
  "leaseVersion": 4,
  "renewalProtocol": "challenge-refresh",
  "singleDeviceLicense": true
}
```

如果还看到：

```text
apiVersion = 2
```

说明 Cloudflare 还没有部署到最新 main，请重新部署。

---

# 23. 第二项检查：Bootstrap

打开：

```text
https://你的项目.pages.dev/bootstrap/v1/config
```

应包含：

```text
apiVersion = 3
leaseVersion = 4
canonicalBaseUrl
leaseHours = 72
renewalProtocol = challenge-refresh
singleDeviceLicense = true
permanentDisplayText = 永久授权
issuedAt
signedPayload
signature
```

Bootstrap 本身也必须通过客户端内置服务器公钥验签。

---

# 24. 第三项检查：管理后台

打开：

```text
https://你的项目.pages.dev/admin
```

登录必须同时需要：

```text
管理员密码
+
Cloudflare Turnstile
```

如果 Turnstile 不显示：

优先检查：

```text
TURNSTILE_SITE_KEY 是否填写正确
Pages 主机名是否加入 Turnstile Hostname Management
当前域名是不是你真正访问的域名
```

---

# 25. 测试永久授权

后台创建：

```text
授权类型：永久
数量：1
```

激活以后客户端签名 Lease 应满足：

```text
licenseType = permanent
isPermanent = true
licenseExpiresAt = null
displayText = 永久授权
countdownRequired = false
```

客户端右下角必须显示：

```text
永久授权
```

不要显示：

```text
剩余 71 小时
```

因为 72 小时只是 Lease 内部刷新周期。

---

# 26. 测试 7 天授权

后台创建：

```text
授权类型：激活后 N 天
有效天数：7
数量：1
```

创建以后先不要激活。

后台应看到：

```text
首次激活：未激活
到期：无
```

第二天、第三天仍然不会自动减少 7 天。

第一次激活以后：

```text
activated_at = 当前首次激活时间
expires_at = 当前时间 + 7 天
```

客户端使用签名 Lease 里的：

```text
licenseExpiresAt
```

显示总授权倒计时。

---

# 27. 客户端倒计时不能只信 Windows 系统时间

为了防止客户把 Windows 时间往回改：

客户端建议保存：

```text
serverTime
licenseExpiresAt
最近一次可信服务器时间
单调时钟基准
```

运行期间优先使用单调计时器计算已经过去的时间。

如果发现：

```text
Windows 当前时间明显早于最近一次可信服务器时间
```

不要因此把授权期限延长。

需要等下次联网重新校准可信时间。

---

# 28. 72 小时正常续期

Lease 快到期时：

第一步：

```text
POST /api/v1/device/challenge
```

发送：

```json
{
  "licenseKey": "CLC-XXXX-XXXX-XXXX-XXXX",
  "deviceId": "你的稳定设备码"
}
```

服务器返回：

```text
nonce
expiresAt
licenseExpiresAt
licenseExpired
```

客户端用本机设备 P-256 私钥签名 nonce。

第二步：

```text
POST /api/v1/license/refresh
```

发送示例：

```json
{
  "licenseKey": "CLC-XXXX-XXXX-XXXX-XXXX",
  "deviceId": "你的稳定设备码",
  "nonce": "服务器刚返回的nonce",
  "signature": "设备私钥签名后的Base64URL",
  "windowsVersion": "Windows 11 24H2",
  "appVersion": "1.0.0"
}
```

验证成功返回新的签名 Lease。

正常情况下每约 72 小时只需要这两次请求。

---

# 29. 授权总期限到期

例如 7 天授权到 0：

```text
客户端立即停止授权功能
```

客户端联网后仍可执行：

```text
challenge
↓
refresh
```

服务器验证原设备私钥以后发现总授权已到期，会返回：

```text
LICENSE_EXPIRED_RELEASED
```

同时：

```text
deviceReleased = true
canActivateNewLicense = true
```

服务器会释放旧设备绑定。

此时设备可以输入新的授权码。

---

# 30. 原授权续期

如果管理员给原来的“激活后 N 天”授权续期：

后台点击：

```text
续期
```

输入：

```text
7
```

代表追加 7 天。

规则：

```text
没过期：从原 expires_at 再加 7 天
已经过期：从当前服务器时间开始加 7 天
```

不会修改第一次：

```text
activated_at
```

如果旧设备还没完成到期释放：

```text
challenge + refresh
```

直接恢复得到新 Lease。

如果旧设备已经释放：

```text
重新输入原授权码
→ activate
```

即可重新绑定。

---

# 31. 管理员换机

后台：

```text
设备管理
→ 找到设备
→ 解绑
```

解绑之后：

```text
原授权码可以绑定新电脑
原电脑可以输入其他授权码
```

授权的第一次激活时间不会因为换机重新开始。

例如：

```text
7 天授权已经使用 3 天
管理员解绑换电脑
```

新电脑继续使用剩余约 4 天。

不会重新获得完整 7 天。

---

# 32. 正式域名

Pages 地址建议永久保留：

```text
https://你的项目.pages.dev
```

正式 API 可以使用：

```text
https://license.example.com
```

在 Pages 项目中添加 Custom Domain 后：

```text
1. 等域名状态正常
2. 访问 https://license.example.com/api/v1/health
3. 确认 API v3
4. 把 license.example.com 加入 Turnstile Hostname Management
5. 打开 https://license.example.com/admin
6. 后台设置中填写 license.example.com
7. 输入管理员密码 + Turnstile 二次验证
8. 保存
```

客户端仍然保留 pages.dev Bootstrap。

这样以后正式域名再次变化，客户端还能通过 Bootstrap 获取新 API 地址。

---

# 33. 不要把这些内容提交到 GitHub

绝对不要提交：

```text
真实 ADMIN_PASSWORD
真实 SESSION_SECRET
真实 TURNSTILE_SECRET
真实 LICENSE_SIGNING_PRIVATE_KEY
cleanc-private.pem
任何 Cloudflare API Token
```

仓库是公开时尤其重要。

如果误提交私钥或 Secret：

```text
不要只删除 GitHub 文件
应该立即生成新密钥/Secret，并在 Cloudflare 中替换
```

---

# 34. 上线前最后检查

逐项确认：

```text
[ ] Cloudflare Pages 已连接 GitHub main
[ ] Build command = npm run build
[ ] Build output directory = public
[ ] D1 Binding 名称严格为 DB
[ ] D1 已重新部署生效
[ ] APP_NAME = CleanC
[ ] LEASE_HOURS = 72
[ ] BOOTSTRAP_BASE_URL = 稳定 pages.dev 地址
[ ] TURNSTILE_SITE_KEY 已设置
[ ] TURNSTILE_SECRET 已 Encrypt
[ ] ADMIN_PASSWORD 已 Encrypt
[ ] SESSION_SECRET 已 Encrypt
[ ] LICENSE_SIGNING_PRIVATE_KEY 已 Encrypt
[ ] /api/v1/health 显示 apiVersion=3
[ ] /api/v1/health 显示 leaseVersion=4
[ ] Bootstrap 可以被客户端公钥验签
[ ] 一个授权码只能绑定 1 台设备
[ ] 一台设备只能占用 1 个授权
[ ] 永久授权右下角显示“永久授权”
[ ] 7 天授权未激活前不会开始倒计时
[ ] 7 天授权首次激活后才计算 expires_at
[ ] 换机不会重新开始授权总时长
[ ] Lease 到期前只执行 challenge + refresh
[ ] 授权总时间到 0 时客户端立即停止授权功能
[ ] 到期联网后能够返回 LICENSE_EXPIRED_RELEASED
[ ] 管理员解绑后可以换机
[ ] 管理员续期后原授权可以继续使用
```

全部通过再作为正式生产环境使用。

---

# 35. 常见报错

## D1_BINDING_MISSING

说明：

```text
Pages 没有绑定 D1
```

处理：

```text
Settings
→ Bindings
→ Add D1 database
→ Variable name 填 DB
→ 重新部署
```

---

## SERVER_NOT_CONFIGURED

说明必要变量/Secret 没填完整。

检查：

```text
TURNSTILE_SITE_KEY
ADMIN_PASSWORD
SESSION_SECRET
TURNSTILE_SECRET
LICENSE_SIGNING_PRIVATE_KEY
```

---

## LOGIN_FAILED

检查：

```text
管理员密码
Turnstile Site Key/Secret 是否配对
当前访问域名是否加入 Turnstile Hostname Management
```

---

## LICENSE_ALREADY_BOUND

说明该授权码已经绑定另一台设备。

处理：

```text
管理员后台解绑原设备
```

不要删除数据库记录。

---

## DEVICE_ALREADY_BOUND

说明这台设备当前还占用其他有效授权。

如果旧授权已到期：

```text
使用旧授权执行 challenge + refresh
```

完成：

```text
LICENSE_EXPIRED_RELEASED
```

之后再输入新授权码。

如果旧授权没到期但确实要换授权：

```text
管理员后台手动解绑
```

---

## ACTIVATION_BUSY

说明同一授权码或同一设备正在发生并发激活。

这不是服务器故障。

这是为了保证：

```text
并发请求只能有一个成功
```

客户端可以稍后重新提交；如果另一个请求已经成功绑定，则后续会得到明确的已绑定错误。

---

# 36. 官方参考

Cloudflare Pages Git Integration：

```text
https://developers.cloudflare.com/pages/configuration/git-integration/github-integration/
```

Cloudflare Pages Functions Bindings / D1 / Variables / Secrets：

```text
https://developers.cloudflare.com/pages/functions/bindings/
```

Cloudflare Turnstile Hostname Management：

```text
https://developers.cloudflare.com/turnstile/additional-configuration/hostname-management/
```

---

# 37. 最终生产协议摘要

```text
API Version: 3
Lease Version: 4
Lease: 72 小时
续期: challenge + refresh
授权码最大设备数: 永远 1
设备最大活动授权数: 永远 1
永久授权显示: 永久授权
时长授权: 首次激活开始计时
到期后: 停止使用 + 设备私钥证明后释放旧绑定
管理员解绑: 允许换机
管理员续期: 保留首次激活时间，只延长到期时间
```
