# 授权服务修复与上线阻断项

## 已确认的故障

2026-09-23 实际请求生产服务：health 返回 200；offline/issue 返回 503，提示离线激活尚未配置。源码中的对应条件是缺少 OFFLINE_RSA_PRIVATE_KEY。在线签名密钥 LICENSE_SIGNING_PRIVATE_KEY 与离线 RSA 密钥不是同一把密钥，不能互相代替。

本次仅使用空 JSON 请求核查配置，没有兑换用户截图中的授权码。

## 已修复

- 缺失密钥、密钥格式错误、数据库故障与无效扫码请求分别返回可诊断的错误码。
- 支持环境配置中使用字面量 `\n` 换行的 PEM；在线激活、续期与 Bootstrap 的签名路径统一处理。
- 新增 GET `/api/v1/offline/readiness`，检查离线 RSA 密钥能否导入；它不是完整的激活验收，也不证明公私钥配对、签名密钥或数据库均正常。
- 限制实际读取的请求字节数；不再先把任意大请求全文读入内存再检查。在线激活同样适用。
- 手机响应丢失后，相同会话和相同授权码可以取得同一结果。不同请求/授权码不能借此读取结果。
- 收据与 issued 状态通过 D1 batch 原子保存；失败后释放本次 pending，允许重试已绑定的设备。突发 worker 中断留下的 pending 仍可能需要重新扫码。
- 手机页支持同一标签刷新恢复会话、25 秒请求超时和重试提示；授权码不存入浏览器存储。
- 增加 SQLite/密码学回归测试与锁文件。测试不替代真实 Cloudflare D1/Pages 部署测试。

## 必须完成的配置修复

1. 找到实际托管 wwz554.ccwu.cc 的 Pages 项目与账号。本次连接账号中未发现该项目，未擅自修改其他网站。
2. 找到原有 RSA-3072 私钥，通过本地检查工具核对它与客户端 `Crypto/offline-public.pem` 一致：

   `node scripts/check-offline-key.mjs <私钥文件路径> <客户端offline-public.pem路径>`

   工具只输出匹配状态和公钥指纹，不输出私钥。不要把私钥提交 Git、粘贴到聊天或打进客户端。
3. 在 Pages 的生产环境 Secret 中配置 `OFFLINE_RSA_PRIVATE_KEY` 并重新部署。参考官方说明：https://developers.cloudflare.com/pages/functions/bindings/#secrets
4. 如果原私钥已经丢失，无法从公钥恢复。必须生成新配对，同时发布更新了公钥的客户端和服务器；只换服务器密钥会让旧客户端继续失败。
5. 用专门测试授权依次验收永久、按天、固定到期、重复提交、过期、禁用、解绑和网络中断；不要使用真实客户授权进行自动测试。

## 商用安全阻断项

旧版 offline-v2 的 16 位码使用客户端持有秘密的 HMAC，不是服务器独占私钥的数字签名。它不能作为抵抗本机攻击者的强商业授权边界。本补丁保留旧版兼容，并未宣称修复了该协议缺陷。

推荐下一步：服务器签名的离线授权文件/完整签名字符串，绑定设备公钥与会话；客户端仅内置验签公钥。旧 16 位码只在受控迁移期兼容。保持任意长离线且同时立即接收远程吊销是无法兼得的，需明确离线有效期政策。

尚未验收：真实 CF 部署、并发压测、灾难恢复、D1 备份恢复、管理员安全与全量历史迁移矩阵。不能把当前修复包标记为完成商用安全认证。
