# Example names only. Never commit real secret values.
# Production values must be configured in Cloudflare Pages > Settings > Variables and Secrets.

# Plain variables
APP_NAME=CleanC
TURNSTILE_SITE_KEY=replace-with-site-key
LEASE_HOURS=72
BOOTSTRAP_BASE_URL=https://your-project.pages.dev

# Encrypted secrets
ADMIN_PASSWORD=change-me
SESSION_SECRET=generate-a-long-random-secret
TURNSTILE_SECRET=turnstile-secret
LICENSE_SIGNING_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nREPLACE_FOR_LOCAL_TESTING_ONLY\n-----END PRIVATE KEY-----"

# DEVICE_PROOF_SECRET is no longer required by the production Pages protocol.
# Production renewal now uses device challenge + ECDSA signature directly.
