# CleanC License Server 生产上线验收清单

> 用途：部署完成后，正式给客户使用之前，按本文件逐项测试。
>
> 当前生产协议：API v3 / Lease v4 / 默认 Lease 72 小时 / challenge + refresh 两步续期。

---

# 一、基础部署检查

## 1. Health

浏览器打开：

```text
https://你的项目.pages.dev/api/v1/health
```

必须看到：

```text
apiVersion = 3
leaseVersion = 4
renewalProtocol = challenge-refresh
singleDeviceLicense = true
```

如果还是 `apiVersion = 2`，说明 Cloudflare 没有部署到最新 main。

## 2. Bootstrap

打开：

```text
https://你的项目.pages.dev/bootstrap/v1/config
```

必须包含：

```text
apiVersion = 3
leaseVersion = 4
canonicalBaseUrl
leaseHours = 72
renewalProtocol = challenge-refresh
singleDeviceLicense = true
permanentDisplayText = 永久授权
signedPayload
signature
```

客户端必须先验证 `signedPayload + signature`，验签通过后才能相信里面的地址和版本。

## 3. 管理后台

打开：

```text
https://你的项目.pages.dev/admin
```

必须满足：

```text
管理员密码正确 + Turnstile 成功 → 可以登录
管理员密码错误 → 不能登录
Turnstile 失败 → 不能登录
```

---

# 二、D1 数据库检查

D1 中应该自动存在：

```text
licenses
devices
audit_logs
system_settings
domain_history
device_challenges
rate_limits
activation_locks
```

不要手工修改这些表结构。

必须确认数据库约束已经生效：

```text
一个 license_id 最多只有一个 revoked_at IS NULL 的设备
一个 device_id 最多只有一个 revoked_at IS NULL 的授权
```

---

# 三、永久授权测试

后台新建一个：

```text
授权类型：永久
数量：1
```

第一次激活以后，签名 Lease 必须满足：

```text
licenseType = permanent
isPermanent = true
licenseExpiresAt = null
displayText = 永久授权
countdownRequired = false
```

客户端右下角必须只显示：

```text
永久授权
```

不能显示：

```text
剩余 72 小时
剩余 3 天
```

注意：Lease 自身仍有 `expiresAt`，默认约 72 小时后静默续租；这个时间不能作为永久授权总期限展示给客户。

---

# 四、7 天授权首次激活计时测试

后台创建：

```text
授权类型：激活后 N 天
N = 7
```

刚创建、尚未激活时，数据库应该是：

```text
activated_at = NULL
expires_at = NULL
```

放置一段时间不使用，也必须保持 NULL。

第一次真正成功激活后：

```text
activated_at = 首次成功激活服务器时间
expires_at ≈ activated_at + 7 天
```

允许秒级差异，但绝不能从“创建授权码时间”开始计算。

---

# 五、重复激活不能重置时间

同一设备、同一授权码再次调用 activate。

应满足：

```text
激活成功或返回当前有效 Lease
activated_at 不变
expires_at 不变
```

不能重新得到完整 7 天。

---

# 六、同一授权码并发抢占测试

准备同一个未使用授权码，让两台不同设备几乎同时激活。

预期：

```text
只有一个设备最终成功绑定
另一个请求返回 ACTIVATION_BUSY 或 LICENSE_ALREADY_BOUND
D1 最终只能有 1 条活动绑定
```

成功设备以“第一个成功取得 D1 授权锁的请求”为准。

---

# 七、同一设备不能占两个授权

设备 A 已经绑定授权码 1。

不解绑的情况下尝试绑定授权码 2。

预期：

```text
DEVICE_ALREADY_BOUND
```

授权码 2 不能占用设备 A。

---

# 八、管理员手动解绑换机测试

设备 A 已绑定授权码 1。

后台：

```text
设备管理 → 解绑
```

解绑后测试：

```text
授权码 1 可以绑定设备 B
设备 A 可以绑定授权码 2
```

如果授权码 1 是 7 天授权：

```text
activated_at 不变
expires_at 不变
```

换机不能获得新的完整 7 天。

---

# 九、设备公钥保护测试

同一 `deviceId + licenseKey`，但换一套新的设备 P-256 私钥/公钥再次激活。

预期：

```text
DEVICE_KEY_MISMATCH
```

不能只知道 deviceId 和授权码就冒充原设备。

---

# 十、72 小时 Lease 测试

激活成功以后检查 Lease：

```text
expiresAt ≈ 当前时间 + 72 小时
```

如果总授权只剩 10 小时，则应该：

```text
expiresAt ≈ 总授权到期时间
```

也就是：

```text
Lease expiresAt = min(当前时间 + 72小时, licenseExpiresAt)
```

永久授权例外：

```text
licenseExpiresAt = null
```

但 Lease 的 `expiresAt` 仍为约 72 小时。

---

# 十一、两步续期测试

客户端调用：

```text
POST /api/v1/device/challenge
```

得到：

```text
nonce
expiresAt
```

用本机设备 P-256 私钥对 nonce 签名，然后：

```text
POST /api/v1/license/refresh
```

提交：

```text
licenseKey
deviceId
nonce
signature
```

成功后必须拿到新的签名 Lease。

生产客户端不要调用：

```text
/api/v1/license/validate
/api/v1/device/verify
```

这两个属于废弃接口。

---

# 十二、nonce 重放攻击测试

同一个 nonce + signature 第一次 refresh 成功后，再重复提交完全相同请求。

第二次必须失败：

```text
CHALLENGE_ALREADY_USED
```

或等价的无效 challenge 错误。

不能重复使用同一个 nonce。

---

# 十三、多 challenge 并发测试

同一设备快速请求两个 challenge：

```text
nonce A
nonce B
```

两个 nonce 在各自过期前都应该保持独立，不应因为第二次 challenge 把第一次仍有效的 nonce 删除。

每个 nonce 只能成功使用一次。

---

# 十四、授权总时间到期测试

可以测试一个很短的测试授权，或在测试数据库中用测试数据模拟。

到达 `licenseExpiresAt` 后客户端必须：

```text
立即停止受授权保护功能
```

不能因为 72 小时 Lease 原来还有剩余时间继续使用。

联网后：

```text
challenge
↓
设备私钥签名
↓
refresh
```

服务器确认仍然过期以后返回：

```text
LICENSE_EXPIRED_RELEASED
deviceReleased = true
canActivateNewLicense = true
```

然后该设备可以输入新的授权码。

---

# 十五、到期与管理员续期同时发生测试

这个是生产边界场景。

设备刚到期准备执行 refresh 的同时，管理员给原授权续期。

预期：

```text
如果管理员续期已经落库，服务器不能再误解绑设备
```

服务器会在释放绑定前重新读取并原子确认数据库中授权仍然过期。

如果已经续期：

```text
继续签发新 Lease
```

---

# 十六、未过期授权续期测试

例如：

```text
原到期：2026-10-01 12:00
续期：7 天
```

预期：

```text
新到期：2026-10-08 12:00 左右
```

也就是从原到期时间向后追加。

---

# 十七、已经过期授权续期测试

例如原授权已经过期 3 天，管理员续期 7 天。

预期：

```text
新到期 = 当前服务器时间 + 7 天
```

不会从已经过去的旧到期时间继续算。

如果旧设备绑定已经通过到期上报释放：

```text
客户重新输入原授权码激活即可
```

---

# 十八、禁用授权后再续期测试

后台先把授权设为：

```text
disabled
```

然后点击“续期”。

预期：

```text
到期时间可以延长
状态仍然必须是 disabled
```

续期本身不能自动恢复客户使用权限。

只有管理员明确点击：

```text
恢复
```

才允许重新变成 active。

---

# 十九、管理员解绑后旧 Lease 行为

管理员手动解绑后，服务器端下一次 challenge/refresh 必须拒绝旧设备。

客户端可能仍然持有之前签出的离线 Lease，所以客户端策略应明确：

```text
Lease 有效期内允许离线使用
下一次在线续租时发现解绑后停止续租
```

这是 72 小时离线可用设计本身的取舍，不是服务端授权时间错误。

---

# 二十、NAT / 同公网 IP 多客户测试

在同一个公司网络、校园网、家庭出口或模拟同公网 IP 环境下，让多台不同设备做正常激活和续租。

预期：

```text
不同设备不会因为共用公网 IP 很快互相触发限流
```

当前生产设计：

```text
激活：公网 IP 宽松总限流 + 单设备细限流
续租：公网 IP 宽松总限流 + 单设备细限流
```

单台异常设备仍然不能高频刷接口。

---

# 二十一、固定到期授权测试

创建固定到期授权，例如：

```text
2026-12-31 23:59:59
```

无论什么时候第一次激活，总到期时间都必须保持这个固定时间。

首次激活只能记录 `activated_at`，不能改变固定 `expires_at`。

---

# 二十二、无效授权类型测试

直接构造管理 API 请求，把 licenseType 改成错误值，例如：

```text
vip_forever
```

预期：

```text
INVALID_LICENSE_TYPE
```

绝不能因为输入错误自动创建成永久授权。

---

# 二十三、永久授权数据库保护测试

永久授权必须：

```text
expires_at = NULL
```

数据库保护会拒绝给永久授权写入总到期时间。

客户端显示必须由签名字段决定：

```text
isPermanent = true
displayText = 永久授权
countdownRequired = false
```

---

# 二十四、后台域名切换测试

正式域名绑定完成后，例如：

```text
https://license.example.com
```

先确认：

```text
https://license.example.com/api/v1/health
```

正常，再把主机名加入 Turnstile Hostname Management，然后在后台保存正式授权地址。

Bootstrap 地址仍建议保留：

```text
https://你的项目.pages.dev/bootstrap/v1/config
```

客户端通过签名 Bootstrap 获取最新 `canonicalBaseUrl`。

---

# 二十五、上线前最终勾选

```text
[ ] GitHub main 最新 Code Check 成功
[ ] Cloudflare Pages 已部署最新 main
[ ] /api/v1/health 显示 API v3 / Lease v4
[ ] Bootstrap 签名可被客户端公钥验证
[ ] D1 Binding 名称严格是 DB
[ ] APP_NAME 已填写
[ ] TURNSTILE_SITE_KEY 已填写
[ ] LEASE_HOURS=72
[ ] BOOTSTRAP_BASE_URL 是稳定 pages.dev 地址
[ ] ADMIN_PASSWORD 使用强密码并 Encrypt
[ ] SESSION_SECRET 使用随机值并 Encrypt
[ ] TURNSTILE_SECRET 已 Encrypt
[ ] LICENSE_SIGNING_PRIVATE_KEY 已 Encrypt
[ ] 私钥没有提交 GitHub
[ ] Windows 客户端只内置服务器公钥
[ ] 永久授权只显示“永久授权”
[ ] 7天授权首次激活前不计时
[ ] 重复激活/换机不重置有效期
[ ] 一个授权码只能 1 台活动设备
[ ] 一个设备只能 1 个活动授权
[ ] challenge + refresh 两步续期正常
[ ] nonce 不可重放
[ ] 总授权到期后客户端立即停止受保护功能
[ ] 到期联网后可安全释放旧绑定
[ ] 管理员续期不会误解除 disabled
[ ] 管理员解绑后可以换机
[ ] NAT 下多客户正常使用不会被共享 IP 轻易误限流
[ ] 固定到期授权不会因首次激活改变总到期时间
```

以上全部通过后，再把客户端正式发给客户。
